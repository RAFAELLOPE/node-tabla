# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar      [número] [requerido]
  -l, --list     Muestra la tabla en la consola [booleano] [requerido] [defecto: false]
  -h, --hasta    Hasta donde llega la multiplicación [número] [requerido] [defecto: 10]
```
